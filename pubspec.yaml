name: expense_tracker
description: A new Flutter project.
publish_to: 'none'
version: 1.1.1+12

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # Firebase - Upgrade to latest for Web support
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  google_sign_in: ^6.2.1
  cloud_firestore: ^6.1.1
  
  # State Management
  provider: ^6.1.1
  
  # UI/Charts
  fl_chart: ^1.1.1
  google_fonts: ^6.1.0
  lucide_icons: ^0.257.0
  intl: ^0.20.2
  
  # Utilities
  uuid: ^4.3.3
  shared_preferences: ^2.2.2

  # Updates
  package_info_plus: ^9.0.0
  http: ^1.2.0
  dio: ^5.4.0
  path_provider: ^2.1.2
  open_filex: ^4.3.2
  flutter_local_notifications: ^17.0.0
  workmanager: ^0.9.0+3
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "web/icons/Icon-512.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
